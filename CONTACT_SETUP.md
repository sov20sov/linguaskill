# دليل إعداد نظام التواصل

## نظرة عامة

تم تحسين نموذج التواصل في الموقع ليكون نظاماً متكاملاً وآمناً يتعامل مع:
- ✅ جمع بيانات المستخدم
- ✅ التحقق من صحة البيانات
- ✅ إرسال البريد الإلكتروني تلقائياً
- ✅ رد واضح للمستخدم

---

## الخطوات المطلوبة للإعداد

### 1. تثبيت المكتبات المطلوبة

```bash
npm install
```

هذا سيقوم بتثبيت جميع المكتبات المطلوبة بما فيها:
- `express` - للخادم
- `nodemailer` - لإرسال البريد الإلكتروني
- `cors` - للسماح بالاتصال من Frontend
- `dotenv` - لإدارة متغيرات البيئة

### 2. إعداد البريد الإلكتروني (Gmail مثال)

#### أ. إنشاء App Password من Gmail

1. اذهب إلى حساب Google الخاص بك
2. قم بتمكين "التحقق بخطوتين" (Two-Step Verification)
3. اذهب إلى: https://myaccount.google.com/apppasswords
4. اختر:
   - **App**: البريد (Mail)
   - **Device**: جهاز آخر (Other)
   - أدخل اسم: "Linguaskill Contact Form"
5. انسخ كلمة المرور المولدة (16 حرف)

#### ب. إعداد ملف البيئة

1. انسخ ملف `.env.example` في مجلد `server/`:
   ```bash
   cp server/.env.example server/.env
   ```

2. افتح `server/.env` واملأ البيانات:

```env
# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:3000

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Recipient Email (البريد الذي ستصل إليه الرسائل)
CONTACT_RECIPIENT_EMAIL=recipient@example.com
```

**ملاحظات مهمة:**
- `SMTP_USER`: بريد Gmail الخاص بك
- `SMTP_PASS`: كلمة المرور المولدة من App Passwords (16 حرف)
- `CONTACT_RECIPIENT_EMAIL`: البريد الذي تريد استقبال الرسائل عليه (يمكن أن يكون نفس `SMTP_USER`)

### 3. تشغيل المشروع

#### في Terminal الأول (Frontend):
```bash
npm run dev
```

#### في Terminal الثاني (Backend):
```bash
npm run dev:server
```

الآن:
- Frontend يعمل على: http://localhost:3000
- Backend يعمل على: http://localhost:3001

---

## كيفية عمل النظام

### 1. واجهة المستخدم (Frontend)

المستخدم يملأ النموذج:
- ✅ الاسم الكامل
- ✅ رقم الهاتف
- ✅ البريد الإلكتروني
- ✅ المستوى المطلوب
- ✅ الرسالة

**التحقق من البيانات:**
- يتم التحقق من جميع الحقول قبل الإرسال
- رسائل خطأ واضحة بالعربية
- تصميم محسّن مع تأثيرات بصرية

### 2. إرسال البيانات (API Request)

عند الضغط على "إرسال الطلب":
- يتم إرسال البيانات إلى `/api/contact`
- البيانات تُرسل بصيغة JSON
- لا يتم كشف أي معلومات حساسة

### 3. الخادم (Backend)

الخادم يقوم بـ:
1. **استقبال البيانات** من Frontend
2. **تنظيف البيانات** من أي أكواد خبيثة
3. **التحقق من الصحة** مرة أخرى
4. **إنشاء رسالة بريد** منظمة وجميلة
5. **إرسال البريد** إلى البريد المحدد

### 4. البريد الإلكتروني

الرسالة المرسلة تحتوي على:
- ✅ جميع بيانات المستخدم
- ✅ تنسيق احترافي بالعربية
- ✅ إمكانية الرد المباشر على المستخدم

### 5. الرد للمستخدم

بعد الإرسال:
- ✅ **نجاح**: رسالة خضراء "تم الإرسال بنجاح"
- ❌ **فشل**: رسالة حمراء مع سبب واضح
- 🔄 **جاري الإرسال**: مؤشر تحميل أثناء الإرسال

---

## الملفات المهمة

### Frontend:
- `components/Contact.tsx` - مكون النموذج
- `services/contactApi.ts` - خدمة API

### Backend:
- `server/index.ts` - الخادم الرئيسي
- `server/api/contact.ts` - معالج طلبات التواصل
- `server/.env` - متغيرات البيئة (لا ترفعه إلى Git!)

---

## الأمان

✅ **ما تم تطبيقه:**
- تنظيف البيانات من الأكواد الخبيثة
- التحقق من صحة البيانات
- عدم كشف تفاصيل الأخطاء للمستخدم
- فصل البيانات الحساسة (SMTP credentials) في ملف `.env`

⚠️ **ملاحظات مهمة:**
- لا ترفع ملف `.env` إلى Git
- استخدم App Password وليس كلمة المرور العادية
- في الإنتاج، استخدم HTTPS

---

## استكشاف الأخطاء

### المشكلة: "حدث خطأ في الاتصال"

**الحل:**
1. تأكد أن Backend يعمل على Port 3001
2. تحقق من `vite.config.ts` أن proxy مضبوط بشكل صحيح
3. تأكد من عدم وجود أخطاء في console

### المشكلة: "فشل إرسال البريد"

**الحل:**
1. تحقق من `SMTP_USER` و `SMTP_PASS` في `.env`
2. تأكد أن App Password صحيح (16 حرف)
3. تحقق من أن "التحقق بخطوتين" مفعّل
4. جرب استخدام بريد آخر للاختبار

### المشكلة: "CORS Error"

**الحل:**
1. تأكد من `FRONTEND_URL` في `.env` صحيح
2. تحقق من إعدادات CORS في `server/index.ts`

---

## للاستخدام في الإنتاج

1. **استخدم خدمة SMTP احترافية:**
   - SendGrid
   - Mailgun
   - AWS SES

2. **أضف Rate Limiting:**
   - لمنع الإرسال العشوائي (Spam)

3. **استخدم HTTPS:**
   - لحماية البيانات أثناء النقل

4. **أضف Logging:**
   - لتسجيل جميع الطلبات

---

## الدعم

إذا واجهت أي مشاكل، تحقق من:
1. Console في المتصفح (F12)
2. Terminal الخاص بالـ Backend
3. ملف `.env` في مجلد `server/`

---

**تم بنجاح! 🎉**

النظام جاهز الآن لاستقبال طلبات التواصل وإرسالها تلقائياً إلى بريدك.
