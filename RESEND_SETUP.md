# 📧 دليل إعداد Resend

## نظرة عامة

تم تحديث النظام لاستخدام **Resend** بدلاً من SMTP لإرسال البريد الإلكتروني. Resend هو خدمة حديثة وموثوقة توفر:

- ✅ إرسال سريع وموثوق
- ✅ واجهة سهلة الاستخدام
- ✅ تتبع الرسائل
- ✅ مجاني حتى 3000 رسالة/شهر
- ✅ لا حاجة لإعدادات SMTP معقدة

---

## الخطوات المطلوبة

### 1. إنشاء حساب Resend

1. اذهب إلى: https://resend.com
2. اضغط على **"Sign Up"** أو **"Get Started"**
3. سجل حساب جديد باستخدام:
   - بريدك الإلكتروني
   - كلمة مرور قوية

---

### 2. الحصول على API Key

1. بعد تسجيل الدخول، اذهب إلى **Dashboard**
2. من القائمة الجانبية، اضغط على **"API Keys"**
3. اضغط على **"Create API Key"**
4. أدخل اسم للـ Key (مثل: "Linguaskill Contact Form")
5. اختر الصلاحيات:
   - ✅ **Sending access** (مطلوب)
   - ❌ **Full access** (اختياري - للأمان استخدم Sending access فقط)
6. اضغط **"Add"**
7. **انسخ API Key الآن** - لن تتمكن من رؤيته مرة أخرى!
   - سيبدو مثل: `re_AbCdEfGhIjKlMnOpQrStUvWxYz123456789`

---

### 3. إضافة Domain (اختياري - للتطوير يمكن تخطيه)

#### للاستخدام في الإنتاج:
1. اذهب إلى **"Domains"** في Dashboard
2. اضغط **"Add Domain"**
3. أدخل domain الخاص بك (مثل: `yourdomain.com`)
4. اتبع التعليمات لإضافة DNS records
5. انتظر حتى يتم التحقق (عادة 5-10 دقائق)

#### للتطوير والاختبار:
- يمكنك استخدام domain Resend المجاني: `onboarding@resend.dev`
- هذا يعمل فقط للتطوير والاختبار

---

### 4. تحديث ملف .env

1. افتح ملف `server/.env`
2. احذف أو علّق إعدادات SMTP القديمة:
   ```env
   # SMTP Configuration (قديم - لم يعد مستخدماً)
   # SMTP_HOST=smtp.gmail.com
   # SMTP_PORT=587
   # SMTP_USER=your-email@gmail.com
   # SMTP_PASS=your-app-password
   ```

3. أضف إعدادات Resend:
   ```env
   # Resend Configuration
   RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   RESEND_FROM_EMAIL=noreply@yourdomain.com
   CONTACT_RECIPIENT_EMAIL=your-email@example.com
   ```

   **مثال حقيقي:**
   ```env
   RESEND_API_KEY=re_AbCdEfGhIjKlMnOpQrStUvWxYz123456789
   RESEND_FROM_EMAIL=noreply@linguaskill.com
   CONTACT_RECIPIENT_EMAIL=admin@linguaskill.com
   ```

   **للتطوير (استخدام domain Resend المجاني):**
   ```env
   RESEND_API_KEY=re_AbCdEfGhIjKlMnOpQrStUvWxYz123456789
   RESEND_FROM_EMAIL=onboarding@resend.dev
   CONTACT_RECIPIENT_EMAIL=your-email@example.com
   ```

---

### 5. تثبيت المكتبة

```powershell
npm install
```

هذا سيقوم بتثبيت مكتبة `resend` تلقائياً.

---

### 6. إعادة تشغيل Backend

1. أوقف Backend الحالي (`Ctrl + C`)
2. أعد تشغيله:
   ```powershell
   npm run dev:server
   ```

3. يجب أن ترى:
   ```
   ✅ Loaded .env from: [مسار]
   🚀 Server is running on port 3001
   ✅ Resend configured:
      API Key: re_AbCd**** (hidden)
      From Email: noreply@yourdomain.com
   📧 Emails will be sent to: your-email@example.com
   ```

---

### 7. اختبار النظام

1. اذهب إلى: http://localhost:3000
2. املأ نموذج التواصل
3. اضغط "إرسال الطلب"
4. تحقق من البريد الإلكتروني - يجب أن تصل الرسالة!

---

## التحقق من الإعداد

### في Terminal (Backend):
يجب أن ترى عند بدء التشغيل:
```
✅ Resend configured:
   API Key: re_xxxx**** (hidden)
   From Email: noreply@yourdomain.com
📧 Emails will be sent to: your-email@example.com
```

### في Resend Dashboard:
1. اذهب إلى **"Emails"** في Dashboard
2. ستجد جميع الرسائل المرسلة
3. يمكنك رؤية:
   - حالة الإرسال (Sent, Delivered, Bounced)
   - وقت الإرسال
   - المستلم

---

## حل المشاكل

### المشكلة: "RESEND_API_KEY is missing"

**الحل:**
1. تأكد من وجود `RESEND_API_KEY` في `server/.env`
2. تأكد من نسخ المفتاح بشكل صحيح (يبدأ بـ `re_`)
3. لا توجد مسافات قبل أو بعد المفتاح

### المشكلة: "From email is missing"

**الحل:**
1. تأكد من وجود `RESEND_FROM_EMAIL` في `.env`
2. إذا لم تحدد domain، استخدم `onboarding@resend.dev` للتطوير

### المشكلة: "Domain not verified"

**الحل:**
1. اذهب إلى Resend Dashboard → Domains
2. تحقق من أن domain محدد ومتحقق منه
3. للتطوير، استخدم `onboarding@resend.dev`

### المشكلة: الرسائل لا تصل

**الحل:**
1. تحقق من Resend Dashboard → Emails
2. ابحث عن الرسالة وافحص حالتها
3. إذا كانت "Bounced"، تحقق من عنوان البريد المستلم
4. تأكد من أن `CONTACT_RECIPIENT_EMAIL` صحيح

---

## المميزات الإضافية

### تتبع الرسائل:
- يمكنك رؤية جميع الرسائل في Resend Dashboard
- معرفة حالة كل رسالة (Sent, Delivered, Opened, Bounced)

### الإحصائيات:
- عدد الرسائل المرسلة
- معدل التسليم
- معدل الفتح

### Webhooks (اختياري):
- يمكنك إعداد webhooks للحصول على تحديثات فورية
- معرفة متى يتم فتح الرسالة أو النقر على رابط

---

## الخطط والأسعار

### Plan المجاني:
- ✅ 3000 رسالة/شهر
- ✅ 100 رسالة/يوم
- ✅ مناسب للمشاريع الصغيرة

### Plan المدفوع:
- يبدأ من $20/شهر
- رسائل غير محدودة
- مميزات إضافية

---

## ملاحظات مهمة

1. **لا ترفع ملف `.env` إلى Git** - يحتوي على معلومات حساسة
2. **احتفظ بـ API Key آمن** - لا تشاركه مع أحد
3. **استخدم domain محدد في الإنتاج** - أكثر احترافية
4. **راقب استخدامك** - Resend Dashboard يعرض الإحصائيات

---

## الدعم

- **Resend Documentation:** https://resend.com/docs
- **Resend Support:** support@resend.com
- **Resend Status:** https://status.resend.com

---

**تم بنجاح! 🎉**

النظام الآن يستخدم Resend لإرسال البريد الإلكتروني بشكل موثوق وسريع.
